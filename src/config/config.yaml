# fx_trading_bot/src/config/config.yaml
# Purpose: Stores configuration settings for live trading and backtesting

# Trading pairs organized by category with multiple timeframes
# 50 major pairs × 3 timeframes (M15, H1, H4) = 150 total backtests
pair_config:
  timeframes: [15, 60, 240]  # M15 (15 min), H1 (60 min), H4 (240 min)
  categories:
    forex:
      symbols: [EURUSD, GBPUSD, USDJPY, USDCHF, AUDUSD, NZDUSD, USDCAD, EURJPY, GBPJPY]
    stocks:
      symbols: [AAPL, MSFT, GOOGL, AMZN, NVDA, TSLA, JPM, V, WMT, JNJ]
    commodities:
      symbols: [XAUUSD, XAGUSD, XPDUSD, XPTUSD, USOIL, UKOIL]
    indices:
      symbols: [USOIL, UKOIL, UK100 , FR40 , JP225, AUS200, HK50]
    crypto:
      symbols: [BTCUSD, ETHUSD]

# Flat pairs list (auto-generated from pair_config on startup)
pairs: []

strategies:
  - name: rsi
    params:
      period: 14
      overbought: 70
      oversold: 30
      volume: 0.01
  - name: macd
    params:
      fast_period: 12
      slow_period: 26
      signal_period: 9
      volume: 0.01
ml:
  - name: random_forest
    params:
      symbol: USDJPY
  - name: lstm
    params:
      symbol: USDJPY
      lookback: 20
mt5:
  login: 297864944
  password: Brian@2025
  server: Exness-MT5Trial9
database:
  path: src/data/market_data.sqlite
data:
  fetch_limit: 1000    # Max rows to fetch from database per query
  fetch_count: 2000    # How many candles to fetch from MT5 API per pair
  min_rows_threshold: 1000  # Minimum rows required per symbol/timeframe before trading
risk_management:
  stop_loss_percent: 1.0
  take_profit_percent: 2.0
  max_open_positions: 10 # Maximum number of concurrent open positions
backtesting:
  data_range: "2025-11-27:2026-01-18"
  optimization:
    rsi:
      period: [10, 12, 14, 16]
      overbought: [60, 70, 80]
      oversold: [20, 30, 40]
    macd:
      fast_period: [8, 12, 16]
      slow_period: [20, 26, 32]
      signal_period: [6, 9, 12]